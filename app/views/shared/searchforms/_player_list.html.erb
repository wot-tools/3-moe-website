<div class="outerFilterBox">
	<div class="filterBox">
		<%= search_form_for @q do |f| %>
			<%= f.search_field :name_cont, placeholder: "Name" %>
			<%= f.search_field :clan_tag_or_clan_name_cont, placeholder: "Clan" %>

			<%= f.submit "Filter"%>
		<% end %>
	</div>
</div>